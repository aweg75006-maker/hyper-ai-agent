# AskHumanTool 使用说明

## 功能概述

`AskHumanTool` 是一个强大的交互式工具，允许AI在无法独立完成任务时向用户寻求帮助。这个功能让AI能够：

1. **向用户提问** - 获取缺失的信息或澄清需求
2. **请求确认** - 在执行敏感操作前获得用户许可
3. **提供选项** - 让用户从多个有效方案中选择偏好方案

## 工具方法

### 1. askHuman

向用户询问开放式问题。

**适用场景：**
- 需要用户的个人偏好或意见
- 缺少完成任务的必要信息
- 需要用户提供特定数据

**示例：**
```
AI: 我需要了解你的技术背景来给出更好的建议
[调用 askHuman 工具，询问用户的问题]
```

### 2. confirmAction

在执行重要操作前请求用户确认。

**适用场景：**
- 准备执行可能影响系统或文件的操作
- 需要用户明确授权的关键步骤
- 涉及敏感数据的操作

**示例：**
```
AI: 我准备删除以下文件，请确认
[调用 confirmAction 工具]
```

### 3. selectOption

向用户展示多个选项并让用户选择。

**适用场景：**
- 存在多个有效解决方案
- 需要用户选择优先级或方法
- 提供不同难度的实现方案

**示例：**
```
AI: 我找到了三种实现方式，请选择
[调用 selectOption 工具，展示选项]
```

## 如何使用

### 方式一：通过AI自动触发

AI会在需要时自动使用AskHuman工具，无需特殊配置。只需在请求中暗示AI可能需要更多信息：

```
用户: 我想去旅游，帮我规划行程

AI: [自动调用 askHuman] 请问你想去哪个城市？预算大概是多少？
用户: 我想去日本，预算2万元
AI: 好的，根据你的需求...
```

### 方式二：明确要求AI询问

可以明确告诉AI在不确定时询问你：

```
用户: 我需要选择一个学习编程的语言。
如果你不确定哪个最适合，请询问我的需求和背景。

AI: [调用 askHuman] 为了给你最合适的建议，我需要了解：
1. 你是否有编程基础？
2. 你想开发什么类型的应用？
```

## 交互界面示例

当AI调用AskHuman工具时，你会看到类似如下的交互界面：

```
==============================================
🤖 AI需要你的帮助
==============================================
问题: 请问你的编程经验水平是什么？
----------------------------------------------
你的回答: [等待你的输入...]
==============================================
```

对于确认操作：

```
==============================================
⚠️  需要确认操作
==============================================
AI准备执行以下操作:
删除文件夹 /tmp/test
----------------------------------------------
是否同意? (yes/no): [等待你的输入...]
==============================================
```

对于选项选择：

```
==============================================
🔀 请选择一个选项
==============================================
问题: 你想使用哪种编程语言？
----------------------------------------------
1. Java
2. Python
3. JavaScript
----------------------------------------------
请输入选项编号 (1-3): [等待你的输入...]
==============================================
```

## 最佳实践

1. **提供清晰的信息** - 当AI询问时，尽量提供详细、明确的回答
2. **合理使用确认** - 对于重要操作，仔细阅读后再确认
3. **诚实表达偏好** - 选择选项时，根据自己的真实需求选择
4. **可以要求AI解释** - 如果不理解问题，可以要求AI重新表述

## 测试

运行测试类来验证功能：

```bash
# 运行所有测试
./mvnw test

# 运行特定的AskHuman测试
./mvnw test -Dtest=AskHumanToolTest

# 运行集成测试（会触发AI询问）
./mvnw test -Dtest=HyperManusTest#testAskHumanFeature
```

**注意：** `testAskHumanFeature` 是一个交互式测试，需要手动输入来测试人机交互功能。

## 技术实现

- **工具类：** `com.yzz.hyperaiagent.tools.AskHumanTool`
- **注册位置：** `ToolRegistration.allTools()`
- **Agent集成：** 已在 `HyperManus` 中启用
- **依赖：** 使用标准的 `java.util.Scanner` 进行控制台输入

## 注意事项

1. **阻塞式交互** - AskHumanTool会阻塞等待用户输入，不适合完全自动化的场景
2. **测试环境** - 在CI/CD环境中可能需要跳过交互式测试
3. **输入格式** - 对于选项选择，请输入数字编号；对于确认，请输入 yes/no 或 y/n
4. **多行输入** - 当前版本支持单行输入，多行内容请分多次回答

## 示例场景

### 场景1：学习计划定制

```
用户: 我想学习编程，帮我制定学习计划

AI: [askHuman] 你的编程基础如何？完全零基础还是有一些经验？
用户: 完全零基础

AI: [selectOption] 你最想开发什么类型的应用？
选项: 网站, 移动应用, 数据分析, 游戏
用户: 1 (网站)

AI: 基于你的零基础和网站开发目标，我推荐从Python开始...
```

### 场景2：技术选型

```
用户: 帮我选择一个数据库

AI: [askHuman] 你的项目规模如何？预期的数据量大概多少？
用户: 中小规模，每天大概新增1000条记录

AI: [confirmAction] 根据你的需求，我推荐使用PostgreSQL。
是否需要我帮你安装配置？
用户: yes

AI: [开始执行安装配置...]
```

## 总结

AskHumanTool让AI从"被动执行"转变为"主动询问"，大大提升了AI的实用性和用户体验。
通过合理的人机协作，AI能够提供更精准、更贴合用户需求的解决方案。